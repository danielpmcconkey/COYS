# Wakeup — 2026-05-25a

## Who you are

You are **Hobson** — Dan's host-side Claude Code instance, the butler.
Read `/home/dan/penthouse-pete/CLAUDE.md` for personality and standing
orders. Read `~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`
for project context across all of Dan's work.

## What this project is

COYS is Dan's Discord bot army. System cron + `claude -p` + discord.py
listeners + a thin Python lib. Each agent runs as its own Linux user with
per-user secrets and Claude OAuth. Repo: `/media/dan/fdrive/codeprojects/COYS/`.

## Where we are

Six agents operational: Scotty, Marcus, Bede, Gabi, Zazu, Pollan. Neil
parked, Thatcher deferred. OpenClaw fully gone since 2026-05-14.

This was a **Marcus session** — three shipped changes, all on `main`:

1. **Marcus watch-sync poller** (`16df0ea`) — closes the taste loop for
   the de-Googled NVIDIA Shield. Dan watches Marcus's queue via SmartTube
   (anonymous, no Google account); the poller reads each played video off
   the Shield's live MediaSession over adb and flips it to `watched` in
   the DB. **Live and proven** — flipped `w6UE1-kfPJ8` end-to-end.
   - Code: `agents/marcus/scripts/watch_sync.py`
   - Service: `systemd/coys-marcus-watch.service` (installed, enabled,
     running as the `marcus` user).
   - Spec it was built from: `HobsonsNotes/marcus-watch-sync-spec-2026-05-24.md` (BD).
2. **Deterministic channel-tier CLI** (`6b1fb71`) — Marcus could already
   change tiers from Discord but only by free-handing inline SQL. Added
   `db.py --find-channel "name"` and `db.py --set-tier <id> <n>`; pointed
   his CLAUDE.md at them.
3. **Per-mode model: Marcus curation → Opus, interactive → Sonnet**
   (`d1bf5c4`) — `CronConfig` gained an optional `model`; `bin/run-agent`
   uses `config.cron.model or config.model`; `bin/listen` untouched. The
   cron prompt now reads `~/.preferences.json` FIRST and applies Dan's
   taste throughout curation.

## What happened last session

Built and shipped all three of the above with Dan, live, in one sitting.

Notable discoveries:
- **Marcus's taste system was already built and seeded.** `~/.preferences.json`
  has real channel_notes/boosts/dampens/taste_notes — but its mtime was
  stuck at Apr 5 (seeded at setup, never updated since). This session
  proved the loop works live: Dan said "move Qroo Paul to tier 1" in
  Discord, and Marcus (Sonnet) both wrote the DB tier change AND added a
  reasoned preferences note. Verified in pgAdmin: tier=1.
- The cron prompt never told Marcus to read preferences (it only lived in
  CLAUDE.md, skippable under multi-step load). Fixed in change #3.
- Hobson was wrong twice and said so: bet the preferences file didn't
  exist (it did), and predicted the tier change would stumble on the
  missing CLI (Marcus improvised around it correctly).

Deferred by choice (YAGNI): multi-Shield support. Dan asked how hard a
second box would be — answer: small (endpoint constant → list + loop;
all matching/dedup/completion logic is already device-agnostic), plus one
TV auth tap per new box. Not building it until a second box exists.

## What's next

**First thing: check whether the first Opus-curated Marcus run landed.**
Change #3 took effect on the **17:00 ET run on 2026-05-24** (the evening
before this wakeup). By the time you read this it's in the past. Eyeball
`#marcus_museum` for that digest and ask:
- Did it run cleanly (one post, no error, under the 900s timeout — Opus is
  slower than Sonnet)? `journalctl` for the cron, and `logs/marcus/`.
- Did the taste actually bite? News block should lean into boosts (space,
  science, tech, finance) and go light on dampens (war, authoritarianism,
  tariffs). If it looks identical to pre-change digests, the lever was the
  prompt, not the model — and we'll want to know that.

No other agreed task. Outstanding items below are carried forward.

## Key resources

Watch-sync (this session):
- `agents/marcus/scripts/watch_sync.py` — the poller (has `--probe`,
  `--once`, `--dry-run`).
- `systemd/coys-marcus-watch.service`
- `HobsonsNotes/marcus-watch-sync-spec-2026-05-24.md` — BD's build spec.

Marcus general:
- `agents/marcus/CLAUDE.md` — persona + interactive commands + taste system.
- `agents/marcus/config.yaml` — model per-mode lives here now.
- `agents/marcus/scripts/db.py` — DB ops + the new tier CLI.
- `/home/marcus/.preferences.json` — the taste store (you can't read it as
  dan; have Dan `cat` it from a marcus shell).

Infra:
- `bin/run-agent` (cron) / `bin/listen` (interactive) / `lib/config.py`.
- `/etc/cron.d/coys` — Scotty 06:00, Marcus 17:00, Bede 00/02/04 ET.

Previous wakeups: `wakeup-2026-05-18a.md`, `wakeup-2026-05-15a.md`,
`wakeup-2026-05-14a.md` (OpenClaw exorcism).

## Standing corrections

- **CLAUDE.md and agent-script (db.py) changes are live immediately for
  interactive Marcus.** `bin/listen` spawns a fresh `claude -p` per Discord
  message that re-reads CLAUDE.md and re-invokes scripts each time — no
  restart needed for those. NEW this session. **Contrast with the next
  item** — they are different surfaces.
- **`bin/listen` itself is a long-running systemd service; edits to it do
  NOT take effect until `systemctl restart coys@<agent>.service`.** And
  `bin/listen` is shared across every interactive agent (Zazu, Gabi,
  Marcus, Pollan) — a restart or behaviour change hits all of them.
- **Marcus's adb runs on a dedicated server port (5038)** so it always
  uses the `marcus` user's authorized key, never colliding with dan's
  interactive adb on 5037. adb auth is per-(server-key, device): **each
  new Shield needs a one-time "Allow USB debugging?" tap on the TV.**
- **You are fenced out of marcus's identity.** `sudo` is blocked by the
  guard hook; `/home/marcus` is `0750`; you have no `openclaw` DB creds.
  So you cannot read `.preferences.json`, `.pgpass`, or query
  `openclaw.marcus` yourself — hand Dan the exact commands to run. Standing
  offer to remove this friction: a read-only `claude` role on the openclaw
  DB (mirroring householdbudget), or a narrow `sudo -iu marcus` read
  whitelist. Dan has not taken it up.
- **Scotty's/Marcus's CLAUDE.md say "scripts are ALREADY BUILT, do NOT
  modify."** That's persona guidance for *them*. Hobson (infra maintainer)
  can and should edit those scripts — but be explicit with Dan about what
  you're touching. Acted on this session (edited `db.py`).
- **"Stopped" is not "disabled."** (In MEMORY.md now — candidate to drop
  from future wakeups; flagged for promotion last seen across 3 sessions.)

## Outstanding items

- **Marcus "drop channel" (`subscribed=false`)** is the one remaining
  improvised DB write in the interactive command table. Same class as the
  tier fix — add a `db.py` verb when convenient.
- **Multi-Shield watch-sync** — only if/when Dan gets a second box. Small
  change + one TV tap per box.
- **Verify post-OpenClaw crons fire cleanly, no double-post** (Scotty
  06:00 / Marcus 17:00 / Bede 00,02,04 ET). Carried forward.
- **Promote Zazu URL-formatting + anti-fabrication rules into Zazu's
  CLAUDE.md** — wording in `wakeup-2026-05-14a.md`. Carried forward.
- **Pollan cron** — four entries designed, never added to `/etc/cron.d/coys`.
- **Yahoo Finance RSS** — withering (1 entry a few sessions ago). Swap if
  it hits zero.
- **Neil** — agent files built, Phase 0 (Discord setup) not started.
- **Thatcher** — deferred; separate repo `/media/dan/fdrive/codeprojects/thatcher/`.
- **Billing block in MEMORY.md** is from 2026-04-04 with stale dates
  (April 17). Dan to revise with current Max-plan / credit truth.
- **Zazu 5-min ceiling** — comfortable today; if `(300s)` timeouts return,
  the bottleneck is real latency and bumping won't help.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
