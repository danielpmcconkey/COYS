# Wakeup — 2026-05-30b

## Who you are

You are **Hobson** — Dan's host-side Claude Code instance, the butler.
Read `/home/dan/penthouse-pete/CLAUDE.md` for personality and standing
orders. Read `~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`
for project context across all of Dan's work.

## What this project is

COYS is Dan's Discord bot army. System cron + `claude -p` + discord.py
listeners + a thin Python lib. Each agent runs as its own Linux user with
per-user secrets and Claude OAuth. Repo: `/media/dan/fdrive/codeprojects/COYS/`.

## Where we are

**Marcus v2 ("DJ Mode") is built, deployed, and proven.** Dan requested a
queue via Discord, Marcus curated four long-form picks, asked which Shield,
and pushed to the upstairs Shield on command. The full flow works end-to-end:
catalog refresh, curation, playlist build, ADB push, conversation memory.

**Two-Shield support is live.** Dan added a second NVIDIA Shield (upstairs,
192.168.50.42). Both `queue_push.py` and `watch_sync.py` have been updated.
The watch-sync service has been restarted and is polling both Shields.

None of the v2 or two-Shield work has been committed to git.

## What happened last session

### ADB authorization for the upstairs Shield

Dan added a second Shield at 192.168.50.42:5555. We authorized marcus's
ADB key against it:

```bash
sudo -u marcus adb -P 5038 connect 192.168.50.42:5555
```

The Shield needed to be awake and have developer/network debugging enabled.
Authorization confirmed with a shell echo test.

### Two-Shield script changes

**`queue_push.py`:** Replaced hardcoded `SHIELD_ENDPOINT` with a `SHIELDS`
dict (`downstairs` → .41, `upstairs` → .42). Added `--shield` CLI flag
(choices: `downstairs`, `upstairs`; default: `downstairs`). Output now
includes which shield received the push.

**`watch_sync.py`:** Same `SHIELDS` dict. `read_smarttube_session()` now
takes an endpoint parameter. `poll_once()` and `run_forever()` iterate
over all Shields each cycle. `--probe` dumps both Shields. Availability
logging tracks each Shield independently.

**Marcus CLAUDE.md:** Updated step 6 (push to Shield) to document the
`--shield` flag and instruct Marcus to ask Dan which Shield if he doesn't
specify.

### Marcus v2 end-to-end proof

Dan asked Marcus for 3-4 long-form videos. Marcus ran the full pipeline —
catalog refresh, taste scores, curation, playlist build — selected four
picks (Atun-Shei, Veritasium, Patrick Boyle, Language Jones, ~125 min),
asked which Shield, and pushed to upstairs on Dan's instruction.

### Home directory access

Still blocked. Dan added himself to agent groups last session but hasn't
logged out/in yet. `/home/marcus/.pgpass` still returns permission denied.

## What's next

1. **Commit the v2 + two-Shield work.** Significant uncommitted changes
   across many files. Nothing has been committed since the v2 build.
2. **Comment out Marcus cron.** Line in `/etc/cron.d/coys` still fires
   at 17:00 ET and errors. Requires sudo — hand Dan the command.
3. **v1 cleanup.** `run_daily.py`, `pick.py`, `digest.py` are vestigial.
   Remove once Dan confirms v2 is stable enough.
4. **Verify preferences persisted.** Check if `~/.preferences.json`
   exists in `/home/marcus/` (requires group access — blocked until
   Dan's logout/login).

## Key resources

**Marcus v2 scripts (modified this session):**
- `agents/marcus/scripts/queue_push.py` — two-Shield support, `--shield` flag
- `agents/marcus/scripts/watch_sync.py` — polls both Shields
- `agents/marcus/CLAUDE.md` — updated for two-Shield workflow

**Marcus v2 (built previous session, unchanged):**
- `agents/marcus/listen` — dedicated listener with conversation memory
- `agents/marcus/config.yaml` — interactive-only, opus 4.6
- `agents/marcus/scripts/refresh_catalog.py` — on-demand catalog maintenance
- `agents/marcus/scripts/curate.py` — curation candidates with taste signals
- `agents/marcus/scripts/taste.py` — channel quality score computation
- `agents/marcus/scripts/discover.py` — YouTube Search API discovery
- `agents/marcus/scripts/build_playlist.py` — playlist builder

**Infrastructure:**
- `systemd/coys-marcus-interactive.service` — dedicated Marcus service
- `systemd/coys-marcus-watch.service` — watch-sync poller (restarted this session)

**Previous wakeup:** `wakeup-2026-05-30a.md`

## Standing corrections

- **CLAUDE.md and agent-script changes are live immediately for interactive
  Marcus.** His listener spawns a fresh `claude -p` per message that re-reads
  CLAUDE.md and scripts each time. No restart needed for those.
- **`agents/marcus/listen` is a long-running systemd service; edits require
  `sudo systemctl restart coys-marcus-interactive.service`.**
- **The shared `bin/listen` is ALSO a long-running service. Edits require
  restarting `coys@<agent>.service` for affected agents.**
- **Marcus's adb runs on a dedicated server port (5038)** to avoid collision
  with Dan's interactive adb on 5037.
- **You are fenced out of marcus's identity** for writes. `sudo` blocked by
  guard hook; `/home/marcus` may now be readable if Dan has logged out and
  back in (group membership added 2026-05-29). Hand Dan exact commands for
  anything that needs to run as marcus.
- **Shield becomes unreachable after router reboots.** Power cycle the Shield
  to restore. Root cause unknown. Applies to both Shields.
- **Model is pinned to `claude-opus-4-6`** in the listener, not the `opus`
  alias. Dan's decision — 4.8 tokenizer burns Max plan quota faster.

## Outstanding items

- **Git commit** — none of the v2 or two-Shield work has been committed.
  Large amount of new and modified files.
- **Marcus cron in `/etc/cron.d/coys`** — still fires at 17:00 ET, errors
  harmlessly. Needs commenting out. Requires sudo.
- **YouTube OAuth app** — may be in GCP test mode (7-day token expiry).
  Verify and publish before heavier interactive use.
- **Shield ADB unreachability after router reboots** — needs root cause
  investigation. Workaround: power cycle the Shield.
- **Pollan cron** — four entries designed, never added to `/etc/cron.d/coys`.
- **Neil** — agent files built, Phase 0 (Discord setup) not started.
- **Dan's home dir group membership** — added but needs logout/login.
  Test by reading `/home/marcus/.pgpass`.
- **v1 cleanup** — `run_daily.py`, `pick.py`, `digest.py` are vestigial.
  Remove once v2 is proven stable.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
