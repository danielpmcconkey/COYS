# Wakeup — 2026-06-14a

## Who you are

You are **Hobson** — Dan's host-side Claude Code instance, the butler.
Read `/home/dan/penthouse-pete/CLAUDE.md` for personality and standing
orders. Read `~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`
for project context across all of Dan's work.

## What this project is

COYS is Dan's Discord bot army. System cron + `claude -p` + discord.py
listeners + a thin Python lib. Each agent runs as its own Linux user with
per-user secrets and Claude OAuth. Repo: `/media/dan/fdrive/codeprojects/COYS/`.

## Where we are

**Marcus v2 ("DJ Mode") is built, deployed, proven, committed, and pushed.**
Commit `089b3af` landed the entire v2 body of work — interactive curation,
two-Shield support, watch-sync fix. The repo is clean.

**Marcus cron is commented out.** The old v1 daily cron in `/etc/cron.d/coys`
(17:00 ET) was firing harmlessly into errors. Now silenced.

**Watch-sync race condition fixed.** `build_playlist.py` resets all `queued`
videos to `new` before each rebuild, which was causing watch_sync to lose
track of videos Dan was watching mid-rebuild. `fetch_queued()` now also
matches recently-queued videos within a 7-day window (`status = 'new' AND
last_queued_at > now() - interval '7 days'`). Service restarted.

## What happened last session

1. **Watch-sync bug diagnosed by Marcus himself.** Dan asked Marcus via
   Discord if he was still tracking watch completions. Marcus was, but then
   Dan reported the last queue was full of already-watched videos. Marcus
   traced it to the reset-and-lose race in `build_playlist.py` /
   `watch_sync.py` and proposed a fix. Hobson refined it (bounded 7-day
   window instead of unbounded `times_queued > 0`) and applied it.

2. **Marcus cron commented out** in `/etc/cron.d/coys`. Dan ran the
   `sudo sed` command.

3. **Watch-sync service restarted.** Dan ran
   `sudo systemctl restart coys-marcus-watch.service`.

4. **Everything committed and pushed.** 19 files, 2,873 insertions.
   Commit `089b3af` on `main`, pushed to GitHub.

(The session also covered non-COYS work: restoring household DNS privacy
after a NAS PSU outage — AdGuard Home, router DHCP, port-53 firewall.
Details in the Shield TV setup wakeup, not here.)

## What's next

No explicit next task from Dan. The immediate backlog:

1. **v1 cleanup.** `run_daily.py`, `pick.py`, `digest.py` are vestigial
   v1 scripts. Remove once Dan confirms v2 is stable enough.
2. **Verify YouTube OAuth app** is published (not GCP test mode with
   7-day token expiry). Could break Marcus silently.
3. **Verify preferences persisted.** Check if `~/.preferences.json`
   exists in `/home/marcus/` (requires Dan's group membership to be
   active — he was added to agent groups 2026-05-29 but may not have
   logged out/in since).

## Key resources

**Marcus v2 scripts:**
- `agents/marcus/listen` — dedicated listener with conversation memory
- `agents/marcus/CLAUDE.md` — v2 DJ mode instructions
- `agents/marcus/config.yaml` — interactive-only, opus 4.6
- `agents/marcus/scripts/build_playlist.py` — playlist builder (contains the reset)
- `agents/marcus/scripts/watch_sync.py` — polls both Shields (contains the fix)
- `agents/marcus/scripts/curate.py` — curation candidates with taste signals
- `agents/marcus/scripts/queue_push.py` — ADB push, `--shield` flag

**Infrastructure:**
- `systemd/coys-marcus-interactive.service` — dedicated Marcus service
- `systemd/coys-marcus-watch.service` — watch-sync poller
- `/etc/cron.d/coys` — Scotty (06:00) and Bede (00/02/04) active; Marcus commented out

**Previous wakeup:** `wakeup-2026-05-30b.md`

## Standing corrections

- **CLAUDE.md and agent-script changes are live immediately for interactive
  Marcus.** His listener spawns a fresh `claude -p` per message that re-reads
  CLAUDE.md and scripts each time. No restart needed for those.
- **`agents/marcus/listen` is a long-running systemd service; edits require
  `sudo systemctl restart coys-marcus-interactive.service`.**
- **The shared `bin/listen` is ALSO a long-running service. Edits require
  restarting `coys@<agent>.service` for affected agents.**
- **Marcus's adb runs on a dedicated server port (5038)** to avoid collision
  with Dan's interactive adb on 5037.
- **You are fenced out of marcus's identity** for writes. `sudo` blocked by
  guard hook; `/home/marcus` may now be readable if Dan has logged out and
  back in (group membership added 2026-05-29). Hand Dan exact commands for
  anything that needs to run as marcus.
- **Shield becomes unreachable after router reboots.** Power cycle the Shield
  to restore. Root cause unknown. Applies to both Shields.
- **Model is pinned to `claude-opus-4-6`** in the listener, not the `opus`
  alias. Dan's decision — 4.8 tokenizer burns Max plan quota faster.

## Outstanding items

- **YouTube OAuth app** — may be in GCP test mode (7-day token expiry).
  Verify and publish before heavier interactive use.
- **Shield ADB unreachability after router reboots** — needs root cause
  investigation. Workaround: power cycle the Shield.
- **Pollan cron** — four entries designed, never added to `/etc/cron.d/coys`.
- **Neil** — agent files built, Phase 0 (Discord setup) not started.
- **Dan's home dir group membership** — added but may need logout/login.
  Test by reading `/home/marcus/.pgpass`.
- **v1 cleanup** — `run_daily.py`, `pick.py`, `digest.py` are vestigial.
  Remove once v2 is proven stable.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
