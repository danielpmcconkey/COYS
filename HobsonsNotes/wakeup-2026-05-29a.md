# Wakeup — 2026-05-29a

## Who you are

You are **Hobson** — Dan's host-side Claude Code instance, the butler.
Read `/home/dan/penthouse-pete/CLAUDE.md` for personality and standing
orders. Read `~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`
for project context across all of Dan's work.

## What this project is

COYS is Dan's Discord bot army. System cron + `claude -p` + discord.py
listeners + a thin Python lib. Each agent runs as its own Linux user with
per-user secrets and Claude OAuth. Repo: `/media/dan/fdrive/codeprojects/COYS/`.

## Where we are

Six agents operational: Scotty, Marcus, Bede, Gabi, Zazu, Pollan.
Neil parked, Thatcher deferred. OpenClaw fully gone since 2026-05-14.

This session was split: **network diagnostics** (first half) then
**Marcus v2 design** (second half).

### Network / DNS overhaul (completed)

Dan's household was generating 507K DNS queries/month on NextDNS.
Investigated, mapped every device on the network (26 devices), and
deployed **AdGuard Home on the Synology DS918 NAS** as the household
DNS server, replacing NextDNS for all LAN devices.

Full details in the Shield TV setup notes:
`/media/dan/fdrive/ai-sandbox/workspace/shield-tv-setup/wakeup.md`
(2026-05-29 section).

Key facts for COYS:
- The Shield's DNS moved from NextDNS (Private DNS / DoT) to AdGuard
  Home on the NAS at `192.168.50.121`. Private DNS is now `off`.
- Shield-specific blocking rules (7 Google/Firebase domains) are in
  AdGuard Home's custom filtering rules with `$client=192.168.50.41`.
- Full blocking parity with the old NextDNS profile — verified.
- **Shield becomes unreachable after router reboots.** Power cycle the
  Shield to restore ADB/ping connectivity. Marcus's watch-sync broke
  today until the Shield was rebooted. Root cause unknown.

### Marcus v2 — DJ Mode (planned, not started)

Dan is unhappy with Marcus. Core complaints:
- Same long-form content from the same few channels every night
- No conversation memory (each Discord message is a cold start)
- Can't take on-the-fly requests ("gimme 7 short ones, skip the news")
- Doesn't learn from watch patterns
- Rigid tier-based curation rules designed for dumb models
- When told to "drop" a channel, Marcus deletes the record — then
  rediscovers and re-adds it later

A full requirements doc and implementation plan were produced this session:
- Requirements: `docs/brainstorms/marcus-v2-requirements.md`
- Plan: `docs/plans/2026-05-29-001-feat-marcus-v2-dj-mode-plan.md`

**Read both documents before doing anything.** They are the source of truth.

## What happened last session

Two distinct threads:

**1. DNS / Network (with Dan, not COYS-specific):**
- Investigated 507K DNS queries on NextDNS — traced to BYOD phone
  (corporate MDM), work laptop (split-tunnel VPN), Roku telemetry,
  Google Home devices, and general IoT chatter. Nothing rogue.
- Mapped all 26 devices on the network with MAC addresses and DHCP
  reservations. Dan unplugged/replugged devices to identify unknowns.
- Deployed AdGuard Home (Docker) on the DS918 NAS. Blocklists: AdGuard
  DNS filter, AdAway, OISD Big, Steven Black. Upstream: Quad9.
- Configured 26 persistent clients with IP-to-name mapping.
- Switched the Shield from NextDNS to AdGuard Home (set
  `private_dns_mode off` via ADB).
- Added 7 Shield-specific blocking rules for Google/Firebase telemetry.
- Updated the Shield TV setup wakeup doc.

**2. Marcus v2 design (brainstorm + plan):**
- Brainstormed the redesign with Dan. Key decisions:
  - Marcus becomes a DJ, not a newspaper editor. On-demand queue building
    triggered by Dan in Discord, not a daily cron.
  - **Kill the daily cron entirely.** Catalog refresh happens on first
    request of the evening (<60 seconds).
  - Keep YouTube playlist as delivery mechanism (tested: SmartTube plays
    playlist URLs natively via ADB intent; individual video intents
    interrupt rather than queue).
  - Taste learning primarily inferred from watch-sync completion data.
  - Content discovery via YouTube Search API.
  - Conversation memory: Marcus-specific listener with DB-backed
    conversation log, 2PM-2AM daily window.
  - Channel blacklist/rejection system (permanent, survives rediscovery).
  - Existing tier data stays as soft context, not hard rules.
  - Marcus never rebuilds the playlist without Dan's explicit trigger.
- Wrote the implementation plan: 9 units, phased delivery.
- **Compound engineering plugin was uninstalled** at Dan's request at the
  end of the session.

### ADB queue delivery — proof of concept results

Tested from this machine (not the sandbox):
- `am start -a android.intent.action.VIEW -d "youtube.com/watch?v=ID"`
  → plays the video immediately. A second intent **interrupts** the first
  (no queuing behavior).
- `am start` with a playlist URL → SmartTube opens the playlist with a
  browsable list and native auto-advance. **This is the delivery mechanism.**
- SmartTube has no queue-specific intent (checked all registered intents).
- Dan's host ADB key is authorized on the Shield (done 2026-05-25,
  confirmed working today after Shield power cycle).

## What's next

**Build Marcus v2.** The plan is at
`docs/plans/2026-05-29-001-feat-marcus-v2-dj-mode-plan.md`. Read it first.

Recommended build order: U1 (schema) → U9 (watch-sync extension) →
U2 (Marcus listener) → U3 (catalog refresh) → U6 (blacklist) →
U4 (taste aggregation) → U5 (curation + queue build) → U7 (discovery) →
U8 (CLAUDE.md rewrite).

**Important:** Dan uninstalled the compound engineering plugin. You will
not have `ce-work`, `ce-plan`, or related skills available. Build from
the plan document directly.

## Key resources

**This session's outputs:**
- `docs/brainstorms/marcus-v2-requirements.md` — what to build
- `docs/plans/2026-05-29-001-feat-marcus-v2-dj-mode-plan.md` — how to build it

**Shield / DNS (not in COYS repo):**
- `/media/dan/fdrive/ai-sandbox/workspace/shield-tv-setup/wakeup.md` — full DNS overhaul details
- `/media/dan/fdrive/ai-sandbox/workspace/shield-tv-setup/RUNBOOK-new-shield.md` — Shield setup runbook

**Marcus current state:**
- `agents/marcus/CLAUDE.md` — current persona + curation instructions (will be rewritten in U8)
- `agents/marcus/config.yaml` — dual-mode config (Opus cron / Sonnet interactive)
- `agents/marcus/scripts/` — all scripts (db.py, run_daily.py, build_playlist.py, watch_sync.py, etc.)
- `HobsonsNotes/marcus-watch-sync-spec-2026-05-24.md` — BD's watch-sync build spec

**Infrastructure:**
- `bin/listen` — shared interactive listener (Marcus will get his own in U2)
- `lib/config.py` — config loader
- `systemd/coys-marcus-watch.service` — watch-sync poller (running)

**Previous wakeup:** `wakeup-2026-05-25a.md`

## Standing corrections

- **CLAUDE.md and agent-script changes are live immediately for interactive
  Marcus.** `bin/listen` spawns a fresh `claude -p` per message that re-reads
  CLAUDE.md and scripts each time. No restart needed for those.
- **`bin/listen` itself is a long-running systemd service; edits require
  `systemctl restart coys@<agent>.service`.** And it's shared — a restart
  hits all interactive agents. The 2026-05-18 timeout incident proved this
  the hard way (seven consecutive Zazu failures).
- **Marcus's adb runs on a dedicated server port (5038)** to avoid collision
  with Dan's interactive adb on 5037. Auth is per-(server-key, device).
- **You are fenced out of marcus's identity.** `sudo` blocked by guard hook;
  `/home/marcus` is 0750; no `openclaw` DB creds. Hand Dan exact commands.
- **Shield becomes unreachable after router reboots.** Power cycle the Shield
  to restore. Discovered 2026-05-29. Root cause unknown.
- **Compound engineering plugin is uninstalled.** Don't reference `ce-work`,
  `ce-plan`, or related skills.

## Outstanding items

- **Marcus v2 build** — the main event. Plan ready, implementation not started.
- **AdGuard Home NAS access:** SSH as `pinkstripes@192.168.50.121`, key
  auth from dan-desktop. Docker: `sudo /volume1/@appstore/Docker/usr/bin/docker`
  (passwordless sudo for docker binary only).
- **Shield ADB unreachability after router reboots** — needs root cause
  investigation. Workaround: power cycle the Shield.
- **NextDNS cleanup** — can downgrade or cancel; no longer needed for
  household DNS. Shield profile `3d223c` can be deleted.
- **Dan's phone** — remove NextDNS Private DNS setting when WiFi is turned
  back on (was off during testing).
- **YouTube OAuth app** — may still be in GCP test mode (7-day token expiry).
  Verify and publish before Marcus v2 increases API usage.
- **Marcus "drop channel" CLI** — `db.py` verb for `subscribed=false`.
  Superseded by the blacklist system in v2 (U6), but needed if any v1
  work happens before v2 lands.
- **Pollan cron** — four entries designed, never added to `/etc/cron.d/coys`.
- **Neil** — agent files built, Phase 0 (Discord setup) not started.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
