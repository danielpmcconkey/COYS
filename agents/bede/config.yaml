name: Bede
emoji: "📜"
mode: cron
model: opus

discord:
  channel_id: "1482354239748571206"
  token_file: "/home/bede/.discord-token"

cron:
  schedule: "0 4,6,8 * * *"  # 00:00, 02:00, 04:00 ET in UTC
  timeout_seconds: 1200
  prompt: "Run your transcript summarization cycle. Discover, summarize, update registry, post report to #archives."
  delivery: self

extra_dirs:
  - /home/dan/penthouse-pete/.transcripts
  - /media/dan/fdrive/ai-sandbox/workspace/.transcripts
  - /media/dan/fdrive/codeprojects/ai-dev-playbook
